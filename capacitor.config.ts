import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.memento.mori.app',
  appName: 'MEMENTO_MORI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
